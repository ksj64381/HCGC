# ── Core library ─────────────────────────────────────────────────────────────
torch>=2.0
torch-geometric>=2.0
numpy
scikit-learn
pybind11          # C++ kernel build tool

# ── Benchmark datasets (benchmark.py only) ───────────────────────────────────
pandas            # torch_geometric.datasets.AMiner
scipy             # ACM fallback loader (optional, HGBDataset preferred)
ogb               # ogbn-mag  (pip install ogb)

# ── Large-graph mini-batch sampling ──────────────────────────────────────────
# Required by NeighborLoader for graphs with >100k nodes (AMiner, ogbn-mag).
# torch-scatter must be installed BEFORE torch-sparse (it is a hard dependency).
# Use the version-specific wheel index to avoid ABI mismatches:
#
#   pip install torch-scatter torch-sparse \
#       -f https://data.pyg.org/whl/torch-X.Y.Z+cuZZZ.html
#
# Find your versions:  python -c "import torch; print(torch.__version__, torch.version.cuda)"
# torch-scatter
# torch-sparse
